name: cosmic
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_animate: ^4.2.0+1
  flutter_svg: ^2.0.7
  fluttertoast: ^3.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/splash/
    - assets/login/
    - assets/home/
    - assets/planets/
    - assets/details/
    - assets/accounts/

